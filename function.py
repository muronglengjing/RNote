from PyQt5.QtWidgets import QFileDialog
import newBook,newPage,newBlock

def save():
    print("save")




def newNB(dialog, ui, book):
    dialog.show()


def newB(dialog, ui, book):
    dialog.show()

    # book["默认笔记本"] = {str(text): set()}
    # print(book)


def newP(dialog, ui, book):
    dialog.show()
    # book["默认笔记本"]["默认分区"].add(str(text))
    # print(book)
